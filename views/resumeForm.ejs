<!DOCTYPE html>
<html>
  <head>
    <title>Create Resume</title>
    <style>
      /* Reset and base styles */
      * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
      }

      body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        background: #f0f2f5;
        padding: 30px 15px;
        color: #333;
      }

      /* Attractive heading */
      .hero {
        text-align: center;
        margin-bottom: 40px;
      }

      .hero h1 {
        font-size: 48px;
        background: linear-gradient(90deg, #007bff, #00c6ff);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        font-weight: 800;
        margin-bottom: 10px;
        letter-spacing: 1px;
      }

      .hero p {
        font-size: 18px;
        color: #555;
      }

      form {
        max-width: 700px;
        margin: auto;
        background: #fff;
        padding: 40px;
        border-radius: 12px;
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
      }

      label {
        display: block;
        margin-top: 20px;
        font-weight: 600;
        margin-bottom: 8px;
        color: #2c3e50;
      }

      input[type="text"],
      input[type="email"],
      input[type="file"],
      select,
      textarea {
        width: 100%;
        padding: 12px 15px;
        border: 1px solid #ccc;
        border-radius: 8px;
        font-size: 15px;
        transition: border 0.3s;
      }

      input:focus,
      textarea:focus,
      select:focus {
        border-color: #007bff;
        outline: none;
      }

      textarea {
        resize: vertical;
      }

      button {
        margin-top: 25px;
        padding: 12px 20px;
        background: #007bff;
        color: white;
        font-size: 16px;
        border: none;
        border-radius: 8px;
        cursor: pointer;
        transition: background 0.3s, transform 0.2s;
      }

      button:hover {
        background: #0056b3;
        transform: scale(1.05);
      }

      small {
        color: #999;
        font-size: 13px;
      }

      @media (max-width: 600px) {
        form {
          padding: 30px 20px;
        }

        .hero h1 {
          font-size: 36px;
        }
      }
    </style>
  </head>
  <body>
    <%- include('partials/header.ejs') %>

    <div class="hero">
      <h1>Build Your Professional Resume</h1>
      <p>Quickly create a modern, attractive resume that stands out</p>
    </div>

    <form action="/create" method="POST" enctype="multipart/form-data">
      <label>Select Profile Picture</label>
      <input type="file" name="photo" accept="image/*" />

      <label>Name <small>(Required)</small></label>
      <input type="text" name="name" placeholder="John Doe" required />

      <label>Email</label>
      <input type="email" name="email" placeholder="john@example.com" />

      <label>Phone</label>
      <input type="text" name="phone" placeholder="+1 234 567 890" />

      <label>Education</label>
      <textarea name="education" rows="3" placeholder="Your educational background"></textarea>

      <label>Experience</label>
      <textarea name="experience" rows="3" placeholder="Your work experience"></textarea>

      <label>Skills <small>(comma separated)</small></label>
      <input type="text" name="skills" placeholder="HTML, CSS, JavaScript, Node.js" />

      <label>Summary</label>
      <textarea name="summary" rows="4" placeholder="Write a short professional summary about yourself"></textarea>

      <label>Select Template</label>
      <select name="template">
        <option value="classic">Classic</option>
        <option value="modern">Modern</option>
        <option value="minimal">Minimal</option>
      </select>

      <button type="submit">Create Resume</button>
    </form>
  </body>
</html>
